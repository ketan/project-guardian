version: 3
systemUnderTest:
  service:
    definitions:
    - definition:
        source:
          filesystem:
            directory: .
        specs:
        - spec:
            id: openapiSpec
            path: openapi.yaml

    runOptions:
      openapi:
        specs:
        - spec:
            id: openapiSpec
            baseUrl: http://192.168.1.33
            securitySchemes:
              bearerAuth:
                type: bearer
                token: secret

    # settings:
      # schemaResiliencyTests: all
